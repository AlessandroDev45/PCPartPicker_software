E2E Playwright tests for Funil Técnico

Setup:

1. Install dependencies:

```bash
npm install
npx playwright install
```

2. Run tests:

```bash
npm run test:e2e
```

The tests will write a JSON report to `./e2e/reports/report.json`.
